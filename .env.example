# Copy this file to .env and fill in real values:
#   cp .env.example .env
#
# All env vars consumed by the app must be declared in apps/web/src/env.ts
# (validated at boot via @t3-oss/env-core + zod). Client-exposed vars must
# be prefixed with VITE_.

# Example:
# VITE_PUBLIC_APP_NAME=redomicile
