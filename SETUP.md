# redomicile — Scaffold Summary

Generated 2026-05-22. This document records exactly what was set up.

## Stack

| Tool | Version | Notes |
|---|---|---|
| Node | v25.8.0 | Required `>=22.12.0` |
| pnpm | **11.2.2** | Upgraded from 10.33.2 via `pnpm self-update` |
| Turborepo | **2.9.14** | Root devDependency, pinned exact |
| TanStack Start (`@tanstack/react-start`) | latest (1.168.x) | Per `apps/web/package.json` |
| TanStack Router | latest (1.170.x) | Generated by scaffolder |
| React / React DOM | ^19.2.0 | |
| Vite | ^8.0.0 | Verified compatible — Start peerDep is `vite >=7.0.0` |
| TypeScript | ^6.0.2 (web) / 6.0.3 (root) | |
| Tailwind CSS | ^4.1.18 | Always enabled by TanStack Start CLI (cannot opt out) |
| Vitest | ^4.1.5 | Included for `pnpm test` in the web app |
| Nitro | nightly | Deployment adapter, configured for `node-server` preset |

## Repository Layout

```
/home/choopk/redomicile/
├── apps/
│   └── web/                  # TanStack Start app — name: @redomicile/web
│       ├── src/
│       │   ├── routes/
│       │   │   ├── __root.tsx
│       │   │   └── index.tsx
│       │   ├── router.tsx
│       │   └── styles.css
│       ├── public/
│       ├── .vscode/
│       ├── .cta.json
│       ├── .gitignore
│       ├── README.md         # TanStack Start app readme (autogenerated)
│       ├── package.json
│       ├── tsconfig.json
│       └── vite.config.ts
├── packages/
│   └── .gitkeep              # Empty — reserved for future shared packages
├── .gitignore
├── .npmrc                    # auto-install-peers=true, strict-peer-dependencies=false
├── package.json              # Root, private, packageManager: pnpm@11.2.2
├── pnpm-workspace.yaml       # apps/* + packages/*
├── README.md                 # Repo overview
├── SETUP.md                  # This file
├── tsconfig.json             # Minimal root TS config
└── turbo.json                # build/dev/lint/check-types/clean pipeline
```

## Root Files

### `package.json`
- Private, `name: "redomicile"`
- `packageManager: "pnpm@11.2.2"`
- `engines.node: ">=22.12.0"`
- Scripts: `dev`, `build`, `lint`, `check-types`, `clean` — all delegate to `turbo run`
- devDependencies: `turbo@2.9.14`, `typescript@6.0.3`

### `pnpm-workspace.yaml`
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

### `turbo.json`
- `ui: "tui"`
- Tasks: `build` (with `^build` dep, outputs `.output/**`, `dist/**`, `.vinxi/**`), `dev` (persistent, no cache), `lint`, `check-types`, `clean`

### `.npmrc`
- `auto-install-peers=true`
- `strict-peer-dependencies=false`

## `apps/web`

Generated with: `pnpm dlx @tanstack/cli@latest create web --framework React --package-manager pnpm --no-git --no-install --no-examples --no-intent --no-toolchain --deployment nitro -y`

Modifications after generation:
- Renamed `"name": "web"` → `"@redomicile/web"`
- Added `"check-types": "tsc --noEmit"` script

Scripts:
- `dev` — `vite dev --port 3000`
- `build` — `vite build`
- `preview` — `vite preview`
- `test` — `vitest run`
- `check-types` — `tsc --noEmit`

## Verification

`pnpm build` succeeded — produced:
- `.output/public/` (client assets, ~316 KB JS gzipped to ~100 KB)
- `.output/server/` (SSR bundle, Nitro `node-server` preset)
- Turbo reported: `1 successful, 1 total — 2.041s`

## Quickstart

```bash
cd /home/choopk/redomicile
pnpm install        # Install all workspaces (already done)
pnpm dev            # Start apps/web on http://localhost:3000
pnpm build          # Build everything via turbo
pnpm check-types    # tsc --noEmit across workspaces
pnpm clean          # Remove build outputs + node_modules
```

## What Was Intentionally Left Out

- No `apps/api`, `apps/docs`, or any second app
- No stub `packages/ui`, `packages/config`, or shared `tsconfig` package — `packages/` is empty
- No CI config (`.github/workflows/`)
- No environment/secret scaffolding
- No additional styling system beyond Tailwind (which the TanStack CLI installs by default)
- No auth, no database, no ORM

## Remaining Manual Steps

- [ ] `git init -b main && git add . && git commit -m "Initial scaffold"` (next step in the original plan)
- [ ] Decide on a deployment target (currently Nitro defaults to `node-server`; switch via the `--deployment` flag or by editing `vite.config.ts` / Nitro config)
- [ ] Add shared packages under `packages/` when patterns emerge across apps
